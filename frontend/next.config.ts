// next.config.ts

import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  experimental: {
    turbo: {
      // Add any specific loader configurations if needed
    },
  },
};

export default nextConfig;
